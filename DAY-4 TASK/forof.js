const resumeData = {
    "name": "Guna D",
    "email": "guna12@gmail.com",
    "location": "chennai"
};

// iterate over arrays, strings
const arr = Object.values(resumeData);

for (const value of arr) {
    console.log(value);
}